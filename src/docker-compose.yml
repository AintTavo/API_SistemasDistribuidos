# ══════════════════════════════════════════════════════════════════
#  El mapa del aventurero — Monolito (API + PWA)
#  Front (Nginx) → app (Express/Socket.IO) → Postgres
#  Credenciales y puertos vía .env (ver .env.example).
#  nginx espera a que "app" esté sano (condition: service_healthy)
#  para evitar 502 durante el arranque.
# ══════════════════════════════════════════════════════════════════

# Nombre de proyecto explícito (evita colisión con otras carpetas "src").
name: mapa-aventurero-api

services:
  # ── Base de datos ──────────────────────────────────────────────
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-mapa-aventurero}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-mapa-aventurero}
      POSTGRES_DB: ${POSTGRES_DB:-mapa-aventurero}
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-mapa-aventurero}"]
      interval: 3s
      timeout: 3s
      retries: 20
    volumes:
      - mapa-aventurero_db:/var/lib/postgresql/data

  # ── Monolito Node (API + Socket.IO) — interno ──────────────────
  app:
    build: ./backend
    restart: unless-stopped
    environment:
      PGHOST: db
      PGUSER: ${POSTGRES_USER:-mapa-aventurero}
      PGPASSWORD: ${POSTGRES_PASSWORD:-mapa-aventurero}
      PGDATABASE: ${POSTGRES_DB:-mapa-aventurero}
      JWT_SECRET: ${JWT_SECRET:-mapa-aventurero-monolith-secret}
      PORT: 3000
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:3000/api/health || exit 1"]
      interval: 5s
      timeout: 3s
      retries: 30
      start_period: 5s
    depends_on:
      db:
        condition: service_healthy

  # ── Nginx: sirve el front (backend/public) y proxya la API ─────
  nginx:
    build:
      context: .
      dockerfile: nginx/Dockerfile
    restart: unless-stopped
    ports:
      - "${WEB_PORT:-8080}:80"
    depends_on:
      app:
        condition: service_healthy

volumes:
  mapa-aventurero_db:
