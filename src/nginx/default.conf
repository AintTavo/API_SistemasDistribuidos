# ── PaTavo (monolito) — Nginx front-end ───────────────────────────
# Sirve el front estático de backend/public y reenvía la API y los
# WebSockets al monolito (servicio interno "app:3000").

server {
    listen 80;
    server_name _;

    # Resolver DNS interno de Docker. Con un proxy_pass por variable, nginx
    # resuelve el host en cada petición (no al arrancar), así NO falla aunque
    # el backend todavía no esté listo (evita "host not found in upstream").
    resolver 127.0.0.11 valid=10s ipv6=off;

    root  /usr/share/nginx/html;
    index index.html;

    gzip on;
    gzip_types text/plain text/css application/json application/javascript application/wasm image/svg+xml;

    # ── API REST ──────────────────────────────────────────────────
    location /api/ {
        set                $upstream_app app;
        proxy_pass         http://$upstream_app:3000;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_read_timeout 60s;
    }

    # ── WebSockets del modo mazmorra (Socket.IO) ──────────────────
    location /socket.io/ {
        set                $upstream_app app;
        proxy_pass         http://$upstream_app:3000;
        proxy_http_version 1.1;
        proxy_set_header   Upgrade           $http_upgrade;
        proxy_set_header   Connection        "upgrade";
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_read_timeout 3600s;
    }

    # ── PWA estática + fallback SPA ───────────────────────────────
    location / {
        try_files $uri $uri/ /index.html;
    }
}
